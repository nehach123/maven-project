 CREATE TABLE "SYSTEM"."USERLIST" 
   (	"UNAME" VARCHAR2(20 BYTE), 
	"PWD" VARCHAR2(20 BYTE), 
	 CONSTRAINT "USERLIST_PK" PRIMARY KEY ("UNAME"));
------------------------------------------------------------------------

CREATE TABLE "SYSTEM"."BANKACCOUNT" 
   (	"ACNO" NUMBER(5,0), 
	"HOLDERNAME" VARCHAR2(20 BYTE), 
	"BALANCE" FLOAT(126), 
	 CONSTRAINT "BANKACCOUNT_PK" PRIMARY KEY ("ACNO"));